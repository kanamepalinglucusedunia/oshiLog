/**
 * App-wide motion standard (Figma Smart Animate style):
 * every animation eases in AND out — no springs, no bounces.
 *
 * - Screen transitions (tab changes): fade + scale, 240ms.
 * - Element motion (indicators, FAB): 200ms.
 */
export const MOTION_SCREEN_MS = 240;
export const MOTION_ELEMENT_MS = 200;
